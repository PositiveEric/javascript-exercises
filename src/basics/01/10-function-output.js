function concatenate(firstWord, secondWord, thirdWord) {
  return firstWord + secondWord + thirdWord;
}

const result = concatenate("Hello", "from", "CODELEX");
console.log(result); // Expected result: "HellofromCODELEX"
